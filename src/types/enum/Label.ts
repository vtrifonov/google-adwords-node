export namespace Label {
    export enum Status {
        ENABLED = 'ENABLED',
        REMOVED = 'REMOVED',
        UNKNOWN = 'UNKNOWN',
    }

    export enum Type {
        TextLabel = 'TextLabel',
    }
}